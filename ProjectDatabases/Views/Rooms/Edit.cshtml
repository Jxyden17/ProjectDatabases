@model ProjectDatabases.Models.Room
@{
	ViewData["Title"] = "Edit Room";
	string? errorMessage = (string)ViewData["ErrorMessage"];
}

<h4>@errorMessage</h4>

<h4>Edit Room</h4>
<form action="/Rooms/Edit" method="POST">
	<div class="form-group py-2">
		<label for="RoomNumber">Room Number:</label>
		<input type="number" id="RoomNumber" name="RoomNumber" class="form-control" required value="@Model?.RoomNumber" />
	</div>

	<div class="form-group py-2">
		<label for="Name">Name:</label>
		<input type="text" id="Name" name="Name" class="form-control" required value="@Model?.Name" />
	</div>

	<div class="form-group py-2">
		<label for="Capacity">Capacity:</label>
		<input type="number" id="Capacity" name="Capacity" class="form-control" min="1" max="10" required value="@Model?.Capacity" />
	</div>

	<div class="form-group py-2">
		<label for="Building">Building:</label>
		<input type="text" id="Building" name="Building" class="form-control" required value="@Model?.Building" />
	</div>

	<button type="submit" class="btn btn-primary">Save</button>
</form>

<a href="/Rooms/Index">Back to list</a>